# --- Секретные ключи для Flask-приложений ---
# Сгенерируйте свои собственные ключи, например, с помощью `openssl rand -hex 32`
DATAMATRIX_SECRET_KEY=your_super_secret_key_for_datamatrix
MANUAL_AGGREGATION_SECRET_KEY=your_super_secret_key_for_manual_aggregation

# --- Настройки для ЛОКАЛЬНОЙ базы данных в Docker ---
# Эти значения будут использованы для создания БД при первом запуске контейнера postgres
DB_NAME=portal_db
DB_USER=portal_user
DB_PASSWORD=a_very_strong_password_123 # ЗАМЕНИТЕ НА СВОЙ НАДЕЖНЫЙ ПАРОЛЬ

# DB_HOST зависит от того, откуда вы подключаетесь:
# - 'localhost' - если запускаете скрипт (например, init_db.py) с вашего компьютера (хоста).
# - 'postgres'  - если ваше приложение работает в другом Docker-контейнере.
# Для локального запуска скриптов оставляем localhost.
DB_HOST=localhost
DB_PORT=5432

# --- Готовая строка подключения (не меняйте) ---
DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}

# --- Имена таблиц (оставьте по умолчанию, если нет причин менять) ---
TABLE_PRODUCTS=products
TABLE_PACKAGES=packages
TABLE_ITEMS=items
TABLE_ORDERS=orders
TABLE_AGGREGATION_TASKS=aggregation_tasks